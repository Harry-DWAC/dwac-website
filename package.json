{
  "name": "dwac-website",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && node scripts/fix-lang-attrs.mjs",
    "start": "next start",
    "lint": "next lint",
    "check:colors": "node scripts/check-color-scale.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:redirects": "node scripts/check-redirects.mjs",
    "check:csp": "node scripts/check-csp.mjs",
    "check": "npm run check:colors && npm run check:links && npm run check:redirects && npm run check:csp"
  },
  "dependencies": {
    "next": "14.2.35",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "glob": "^13.0.6",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
